{
  "name": "meshline-protocol",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "check": "node scripts/check.mjs",
    "check:current": "node scripts/check.mjs --require-current",
    "review": "node scripts/review-translations.mjs",
    "test": "npm run check"
  },
  "devDependencies": {
    "github-slugger": "2.0.0",
    "marked": "18.0.13"
  }
}
